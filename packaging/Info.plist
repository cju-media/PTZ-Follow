<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>PTZ Follow</string>
    <key>CFBundleDisplayName</key>
    <string>PTZ Follow</string>
    <key>CFBundleIdentifier</key>
    <string>com.ptzfollow.app</string>
    <key>CFBundleVersion</key>
    <string>1.0.0</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleExecutable</key>
    <string>ptz-tracker</string>
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <key>LSMinimumSystemVersion</key>
    <string>11.0</string>
    <key>NSHighResolutionCapable</key>
    <true/>
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.video</string>
    <!-- The app is intentionally headless (its only UI is the browser tab server.js opens via
         "open()", plus the in-page "Open Console"/"Quit App" controls) - it never creates an
         actual window. Without this key, macOS treats it as a normal foreground app, gives it a
         Dock icon, and bounces that icon indefinitely since a windowless app never signals
         "finished launching" the way Launch Services expects - from the user's side this looks
         exactly like "the app never opened" even when the server underneath started up fine. -->
    <key>LSUIElement</key>
    <true/>
</dict>
</plist>
